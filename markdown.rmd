---
title: "R Notebook"
output: html_document
---

```{r, setup, include=FALSE}
# This is a code chunk
```


```{r}
library(dplyr)
library(ggplot2)
```

```{r, libraries}
df <- read_csv('tracklist.csv')
head(df)
```
position the caret at any line or the code chunk, then click "+".


The code chunk appears:
```{r, data}
# Read data from airport.csv
df <- read_csv('airports.csv')
# Show first few rows
head(df)
```
#
```{r}

# Delete NA in column
df <- df %>% filter(!is.na(lat))
# Mean of column
mean(df$lat)


```

```{r}

```

